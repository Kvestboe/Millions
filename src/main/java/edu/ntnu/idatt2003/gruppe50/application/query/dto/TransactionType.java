package edu.ntnu.idatt2003.gruppe50.application.query.dto;

/**
 * Transaction categories exposed to the UI layer.
 */
public enum TransactionType {
  /**
   * A transaction where shares are bought.
   */
  PURCHASE,

  /**
   * A transaction where shares are sold.
   */
  SALE
}
