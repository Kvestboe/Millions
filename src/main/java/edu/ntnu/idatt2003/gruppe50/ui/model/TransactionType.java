package edu.ntnu.idatt2003.gruppe50.ui.model;

/** UI transaction categories. */
public enum TransactionType {
  /** Transaction where shares were bought. */
  PURCHASE,

  /** Transaction where shares were sold. */
  SALE
}